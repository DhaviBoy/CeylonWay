{
  "name": "ceylonway-backend",
  "version": "1.0.0",
  "description": "Backend API for Ceylonway travel platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "node --check server.js && node --check config/db.js && node --check controllers/authController.js && node --check middleware/auth.js && node --check middleware/upload.js && node --check models/Location.js && node --check models/Property.js && node --check models/User.js && node --check routes/auth.js && node --check routes/dataRoutes.js && node --check seeds/seed.js"
  },
  "keywords": [
    "travel",
    "api",
    "ceylon"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.3",
    "multer": "^2.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
